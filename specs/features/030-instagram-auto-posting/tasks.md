# Feature: Instagram Auto-Posting — Tasks

- [ ] Backend: `app/services/instagram.py` (Protocol + Null/GraphApi
      implementations + factory).
- [ ] Backend: `Store.approve_submission` return type change (`None` →
      `SubmissionApprovalResult | None`) in the protocol, `LocalStore`,
      `DynamoStore`.
- [ ] Backend: wire the scheduled-event post into
      `routers/donation_events.py`'s `create_donation_event`.
- [ ] Backend: wire the completed-event post into
      `routers/admin.py`'s `approve_submission`.
- [ ] Frontend: `public/instagram-scheduled-event.png` (generated once,
      not regenerated by any build step).
- [ ] Infra: `instagram_access_token` / `instagram_business_account_id`
      variables in `modules/api`, wired through `envs/prod`.
- [ ] Manual (founder, one-time, outside this repo): convert the
      @millionmealclub Instagram account to Business/Creator, link it to
      a Facebook Page, create a Meta Developer App, generate a long-lived
      Page access token with `instagram_content_publish` +
      `pages_show_list` permissions, find the Instagram Business Account
      ID — then add both to `infra/envs/prod/secrets.auto.tfvars` and
      apply.
- [ ] Manual test once credentials are live: schedule a real donation
      event, confirm a post appears on @millionmealclub with the branded
      graphic; approve a real submission, confirm a post appears with the
      delivery photo.
