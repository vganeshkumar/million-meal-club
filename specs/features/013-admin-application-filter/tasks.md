# Feature: Admin Application Filter — Tasks

- [ ] Frontend: `AdminSignoff.tsx` — `filter` state + segmented All/Donor/
      Volunteer control in `PendingApplications`, list narrowed to
      `visible`.
- [ ] Frontend test (`frontend/e2e/admin-application-filter.spec.ts`):
      seed one donor + one volunteer application, verify each filter
      option shows the right subset.
